#ifndef ACTIVATION_H
#define ACTIVATION_H

#include <string>

class Activation {
public:
    std::string name;

    /**
     * @brief Activation object constructor
     * 
     * @param name Activation function name
     */
    Activation();
};

#endif